extend type Mutation {
    registerUser(username: String!, email: String!, role: String!, password: String!, fullName: String!, regNo: String!, department: String!): User
    loginUser(username: String!, password: String!): String
}

extend type Query {
    isUserNameExists(username: String!): Boolean
}