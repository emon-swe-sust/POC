type User {
    id: ID!
    username: String!
    email: String!
    role: String
    password: String!
}

type Query {
    getAllUsers: [User]
    getUserById(id: String!): User
}

type Mutation {
    addUser(name: String!, email: String!): User
}